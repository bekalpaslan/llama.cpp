---
phase: 05-hub-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [HUB-01, HUB-02]

must_haves:
  truths:
    - "A user visiting the repo root can see all four worker types (LLM, STT, TTS Voice Mixing, Voice Cloning) listed with descriptions"
    - "Each worker entry includes a Docker image reference or build command, the directory path within the repo, and a link to its hub.json presets"
    - "The LLM (llama.cpp) worker section retains its existing deployment and usage documentation"
    - "A user can navigate from the registry to any worker's directory and understand how to deploy it on RunPod"
  artifacts:
    - path: "README.md"
      provides: "Central hub registry linking all worker types"
      min_lines: 80
      contains: "worker-whisper"
  key_links:
    - from: "README.md"
      to: "worker-whisper/"
      via: "markdown link in Workers section"
      pattern: "worker-whisper"
    - from: "README.md"
      to: "worker-voice-clone/"
      via: "markdown link in Workers section"
      pattern: "worker-voice-clone"
    - from: "README.md"
      to: "tts-voice-mixing/"
      via: "markdown link in Workers section"
      pattern: "tts-voice-mixing"
    - from: "README.md"
      to: ".runpod/hub.json"
      via: "markdown link in LLM section"
      pattern: "\\.runpod/hub\\.json"
---

<objective>
Rewrite the repo README.md to serve as a central hub registry that links all four worker types (LLM, STT, TTS Voice Mixing, Voice Cloning) with descriptions, deployment info, and directory paths.

Purpose: Users landing on this repo need to discover and navigate to all available RunPod workers from a single entry point, not just the llama.cpp LLM worker.
Output: Updated README.md that functions as the RunPod Workers Hub registry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore and rewrite README.md as hub registry</name>
  <files>README.md</files>
  <action>
The README.md was deleted from the working tree. Restore it and rewrite it to serve as the RunPod Workers Hub registry. The new README must contain these sections in order:

1. **Title and tagline**: "RunPod Workers Hub" -- one-click AI inference workers for RunPod Serverless. One repo, multiple worker types covering LLM, speech-to-text, TTS voice mixing, and voice cloning.

2. **Workers overview table**: A table listing all 4 worker types with columns: Worker, Description, Directory, GPU Presets count. Data:
   - LLM (llama.cpp): Serve any GGUF model via llama.cpp with OpenAI-compatible API. Directory: repo root (handler.py, Dockerfile). 36 presets.
   - Speech-to-Text (Whisper): Transcription with faster-whisper, diarization, subtitles. Directory: worker-whisper/. 3 presets.
   - TTS Voice Mixing (Kokoro): Weighted voice blending for Kokoro TTS engine. Directory: tts-voice-mixing/. Module (patches existing TTS worker).
   - Voice Cloning (Chatterbox): Zero-shot voice cloning from 5-30s reference audio, 23 languages. Directory: worker-voice-clone/. 3 presets.

3. **Per-worker sections** -- for each worker, a subsection with:
   - Brief feature list (bullet points)
   - Quick deploy instructions (Docker image or build command, key env vars)
   - Link to the hub.json presets file
   - Link to the worker directory for full documentation

   Specific content per worker:

   **LLM (llama.cpp):**
   - Features: Any GGUF model, auto-detection of quantization, OpenAI-compatible API, streaming, flash attention, KV cache quantization, network volume caching
   - Deploy: `alpaslanbek/llamacpp-worker:latest`, set HF_REPO_ID env var
   - Key env vars table: HF_REPO_ID (required), HF_FILENAME (auto-detect), MODEL_NAME, CTX_SIZE (4096), N_GPU_LAYERS (99), FLASH_ATTN (on), EXTRA_ARGS
   - Presets link: .runpod/hub.json (36 presets -- Qwen 3.5, DeepSeek R1, Llama 3/4, Mistral, Phi-4, Gemma 3, etc.)
   - Input formats: Preserve the 3 input format examples from the old README (Chat Messages, Simple Prompt, OpenAI Passthrough) plus streaming note

   **Speech-to-Text (Whisper):**
   - Features: faster-whisper engine, word-level timestamps, SRT/VTT subtitle output, auto language detection (50+ languages), translation to English, speaker diarization (WhisperX + pyannote), batch processing, VAD anti-hallucination
   - Deploy: Build from worker-whisper/Dockerfile, set MODEL_SIZE env var
   - Key env vars: MODEL_SIZE (large-v3-turbo), COMPUTE_TYPE (int8), MODEL_NAME, HF_TOKEN (optional, for diarization)
   - Presets link: worker-whisper/.runpod/hub.json (3 presets: turbo INT8 on T4, large-v3 FP16 on A4000, turbo+diarize on A4000)

   **TTS Voice Mixing (Kokoro):**
   - Features: Blend 2-5 Kokoro voices with weighted mixing, colon-weight syntax (af_heart:70,af_bella:30), same-language validation, auto-normalization of weights
   - Deploy: Module -- copy voice_blend.py and patches/ into existing TTS worker, rebuild. See tts-voice-mixing/README.md for full integration steps.
   - No hub.json (it patches an existing worker, not a standalone deployment)

   **Voice Cloning (Chatterbox):**
   - Features: Zero-shot voice cloning, 23 languages (cross-lingual synthesis), reference audio quality validation (duration, sample rate, SNR), 48kHz WAV/MP3 output, paralinguistic tags ([laugh], [cough]), emotion/expressiveness control
   - Deploy: Build from worker-voice-clone/Dockerfile, set MODEL_VARIANT env var
   - Key env vars: MODEL_VARIANT (multilingual/turbo/original), MODEL_NAME
   - Presets link: worker-voice-clone/.runpod/hub.json (3 presets: Turbo English on T4, Multilingual on A4000, Original emotion-control on A4000)

4. **Development section**: Brief instructions for building each worker locally:
   - LLM: `docker build --platform linux/amd64 -t alpaslanbek/llamacpp-worker:latest .`
   - STT: `cd worker-whisper && docker build --platform linux/amd64 -t whisper-stt:latest .`
   - Voice Clone: `cd worker-voice-clone && docker build --platform linux/amd64 -t voice-clone:latest .`

5. **License**: MIT

Keep the tone concise and technical. No marketing fluff. Use consistent formatting across all worker sections.
  </action>
  <verify>
    <automated>cd C:/Users/alpas/IdeaProjects/runpod && test -f README.md && grep -c "worker-whisper" README.md && grep -c "worker-voice-clone" README.md && grep -c "tts-voice-mixing" README.md && grep -c "llama" README.md && echo "ALL WORKERS REFERENCED"</automated>
  </verify>
  <done>README.md exists in repo root, contains sections for all 4 worker types (LLM, STT, TTS Voice Mixing, Voice Cloning) with descriptions, deployment instructions, env var tables, and links to hub.json files and worker directories</done>
</task>

<task type="auto">
  <name>Task 2: Validate registry completeness and correctness</name>
  <files>README.md</files>
  <action>
Validate the README.md hub registry by checking:

1. All 4 worker directory paths referenced actually exist in the repo:
   - worker-whisper/ exists
   - worker-voice-clone/ exists
   - tts-voice-mixing/ exists
   - handler.py exists (llama.cpp root)

2. All 3 hub.json paths referenced actually exist:
   - .runpod/hub.json (llama.cpp -- exists in git, check via `git show HEAD:.runpod/hub.json`)
   - worker-whisper/.runpod/hub.json
   - worker-voice-clone/.runpod/hub.json

3. Preset counts match actual hub.json files:
   - LLM: 36 presets
   - STT: 3 presets
   - Voice Clone: 3 presets

4. README is well-formed markdown (no broken links syntax, consistent heading levels, tables render correctly)

If any discrepancy found, fix it in README.md. If .runpod/hub.json is missing from the working tree (deleted), note in README that the llama.cpp hub.json is at `.runpod/hub.json` in the repo (it exists in git history and should be restored -- restore it with `git restore .runpod/hub.json`).
  </action>
  <verify>
    <automated>cd C:/Users/alpas/IdeaProjects/runpod && test -f README.md && test -d worker-whisper && test -d worker-voice-clone && test -d tts-voice-mixing && test -f worker-whisper/.runpod/hub.json && test -f worker-voice-clone/.runpod/hub.json && echo "ALL PATHS VALID"</automated>
  </verify>
  <done>All directory paths and hub.json paths referenced in README.md point to existing locations in the repo. Preset counts are accurate. README renders as well-formed markdown.</done>
</task>

</tasks>

<verification>
- README.md exists in repo root
- README.md references all 4 worker types: LLM (llama.cpp), STT (Whisper), TTS Voice Mixing (Kokoro), Voice Cloning (Chatterbox)
- Each worker section includes: description, deployment info, key env vars, and hub.json/directory links
- All referenced paths (worker-whisper/, worker-voice-clone/, tts-voice-mixing/, .runpod/hub.json) exist in the repo
- Preset counts (36, 3, 3) match actual hub.json files
</verification>

<success_criteria>
A user visiting the repo can see all 4 worker types, understand what each does, and navigate to deployment instructions -- all from the single README.md hub registry.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-registry/05-01-SUMMARY.md`
</output>
