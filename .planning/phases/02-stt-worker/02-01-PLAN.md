---
phase: 02-stt-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker-whisper/pyproject.toml
  - worker-whisper/download_model.py
  - worker-whisper/audio_utils.py
  - worker-whisper/transcribe.py
  - worker-whisper/format_output.py
  - worker-whisper/tests/__init__.py
  - worker-whisper/tests/conftest.py
  - worker-whisper/tests/test_transcribe.py
  - worker-whisper/tests/test_format_output.py
autonomous: true
requirements: [STT-01, STT-02, STT-03, STT-04, STT-05, STT-06]

must_haves:
  truths:
    - "transcribe_audio() accepts a model + audio path and returns segments with word-level timestamps, language, and duration"
    - "VAD filter is enabled by default to prevent Whisper hallucination on silence"
    - "Segments can be converted to SRT, VTT, or plain text format"
    - "Language auto-detection returns detected language code and probability"
    - "Translation task ('translate') is supported alongside transcription"
  artifacts:
    - path: "worker-whisper/transcribe.py"
      provides: "Core transcription logic wrapping faster-whisper"
      exports: ["transcribe_audio"]
    - path: "worker-whisper/format_output.py"
      provides: "SRT/VTT/text output formatting"
      exports: ["segments_to_srt", "segments_to_vtt", "segments_to_text", "format_timestamp_srt", "format_timestamp_vtt"]
    - path: "worker-whisper/tests/test_transcribe.py"
      provides: "Unit tests for transcription with mocked WhisperModel"
    - path: "worker-whisper/tests/test_format_output.py"
      provides: "Unit tests for all output formats"
    - path: "worker-whisper/pyproject.toml"
      provides: "Project config with pytest settings"
  key_links:
    - from: "worker-whisper/transcribe.py"
      to: "faster_whisper.WhisperModel"
      via: "model.transcribe() call"
      pattern: "model\\.transcribe\\("
    - from: "worker-whisper/transcribe.py"
      to: "segments list[dict]"
      via: "generator materialization"
      pattern: "segments\\.append"
---

<objective>
Copy the worker-template into worker-whisper, set up project configuration and test scaffolding, then implement the core transcription engine (transcribe.py) and output format utilities (format_output.py) with full test coverage.

Purpose: Establish the worker-whisper directory with all shared utilities from Phase 1, then build the core STT logic that the handler will wire up in Plan 02.
Output: Working transcribe.py and format_output.py modules with passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stt-worker/02-RESEARCH.md
@.planning/phases/01-shared-worker-foundation/01-01-SUMMARY.md
@.planning/phases/01-shared-worker-foundation/01-02-SUMMARY.md
@worker-template/download_model.py
@worker-template/audio_utils.py
@worker-template/handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy template and set up project scaffold with test fixtures</name>
  <files>
    worker-whisper/pyproject.toml,
    worker-whisper/download_model.py,
    worker-whisper/audio_utils.py,
    worker-whisper/tests/__init__.py,
    worker-whisper/tests/conftest.py
  </files>
  <action>
1. Copy `worker-template/download_model.py` and `worker-template/audio_utils.py` into `worker-whisper/`. These are the shared utilities from Phase 1 -- copy them verbatim, no modifications.

2. Create `worker-whisper/pyproject.toml` with:
   - `[project]` section: name = "worker-whisper", version = "0.1.0", requires-python = ">=3.10"
   - `[project.optional-dependencies]` dev section: pytest>=7.0
   - `[tool.pytest.ini_options]` section: testpaths = ["tests"], python_files = "test_*.py"

3. Create `worker-whisper/tests/__init__.py` (empty file).

4. Create `worker-whisper/tests/conftest.py` with shared fixtures:
   - `mock_whisper_model`: A mock object that mimics `faster_whisper.WhisperModel`. Its `transcribe()` method should return a tuple of `(segments_generator, info)` where:
     - `segments_generator` yields mock segment objects with attributes: `id`, `start`, `end`, `text`, `avg_logprob`, `no_speech_prob`, and `words` (list of mock word objects with `start`, `end`, `word`, `probability`)
     - `info` is a mock object with attributes: `language="en"`, `language_probability=0.98`, `duration=10.5`, `duration_after_vad=9.8`
   - `sample_segments`: A list of pre-built segment dicts (already materialized, as they would appear after transcribe_audio processes them). Include 3 segments with word-level timestamps, covering a ~10 second audio span.
   - `sample_segments_multi_language`: Same structure but with `language="es"` in the info mock.
   - Use `unittest.mock.MagicMock` for mock objects, `@pytest.fixture` decorator for all fixtures.
  </action>
  <verify>
    <automated>cd worker-whisper && python -m pytest tests/ -x -q 2>&1 | head -5</automated>
  </verify>
  <done>worker-whisper/ directory exists with copied utilities, pyproject.toml, and test fixtures. pytest discovers the tests directory with no errors (0 tests collected is fine at this stage).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transcribe.py and format_output.py with tests</name>
  <files>
    worker-whisper/transcribe.py,
    worker-whisper/format_output.py,
    worker-whisper/tests/test_transcribe.py,
    worker-whisper/tests/test_format_output.py
  </files>
  <behavior>
    test_transcribe.py:
    - test_transcribe_basic: transcribe_audio() with mock model returns dict with keys 'segments', 'language', 'language_probability', 'duration', 'duration_after_vad'
    - test_word_timestamps: returned segments contain 'words' list with start/end/word/probability fields
    - test_vad_default_enabled: model.transcribe is called with vad_filter=True by default
    - test_vad_can_be_disabled: passing vad_filter=False forwards to model.transcribe
    - test_language_detection: result contains language and language_probability from model info
    - test_translate_task: passing task="translate" forwards to model.transcribe
    - test_segments_materialized: segments are a list (not generator), can be iterated multiple times
    - test_condition_on_previous_text_false: model.transcribe is called with condition_on_previous_text=False

    test_format_output.py:
    - test_format_timestamp_srt: format_timestamp_srt(3661.5) returns "01:01:01,500"
    - test_format_timestamp_vtt: format_timestamp_vtt(3661.5) returns "01:01:01.500"
    - test_segments_to_srt: produces valid SRT with numbered entries, timestamps, and text
    - test_segments_to_vtt: produces valid VTT starting with "WEBVTT" header
    - test_segments_to_text: joins segment text with spaces, strips whitespace
    - test_srt_empty_segments: returns empty string for empty list
    - test_vtt_empty_segments: returns "WEBVTT\n\n" for empty list
    - test_text_empty_segments: returns empty string for empty list
  </behavior>
  <action>
**RED phase:** Write all tests in test_transcribe.py and test_format_output.py first. Tests should use the mock_whisper_model and sample_segments fixtures from conftest.py. Run tests -- they must all FAIL (modules don't exist yet).

**GREEN phase:**

Create `worker-whisper/transcribe.py`:
- Function `transcribe_audio(model, audio_path, language=None, task="transcribe", word_timestamps=True, vad_filter=True, beam_size=5, temperature=0.0) -> dict`
- Calls `model.transcribe()` with all parameters, plus `condition_on_previous_text=False` and `vad_parameters=dict(min_silence_duration_ms=500)`
- Immediately materializes the segment generator to a list of dicts
- Each segment dict has keys: id, start, end, text, avg_logprob, no_speech_prob, and optionally words
- Words list contains dicts with: start, end, word, probability
- All float values rounded: timestamps to 3 decimals, probabilities to 4 decimals
- Returns dict with: segments, language, language_probability, duration, duration_after_vad
- Follow the exact code pattern from 02-RESEARCH.md "Complete Transcription Flow" section

Create `worker-whisper/format_output.py`:
- `format_timestamp_srt(seconds: float) -> str`: HH:MM:SS,mmm format
- `format_timestamp_vtt(seconds: float) -> str`: HH:MM:SS.mmm format
- `segments_to_srt(segments: list) -> str`: numbered SRT entries
- `segments_to_vtt(segments: list) -> str`: WEBVTT header + entries
- `segments_to_text(segments: list) -> str`: space-joined stripped text
- Follow the exact code pattern from 02-RESEARCH.md "Pattern 3: Output Format Dispatch" section

Run tests -- all must PASS.
  </action>
  <verify>
    <automated>cd worker-whisper && python -m pytest tests/test_transcribe.py tests/test_format_output.py -v</automated>
  </verify>
  <done>transcribe_audio() returns properly structured dict with segments, word timestamps, language info. All 3 output formats (SRT, VTT, text) produce correct output. All tests pass.</done>
</task>

</tasks>

<verification>
cd worker-whisper && python -m pytest tests/ -v
</verification>

<success_criteria>
- worker-whisper/ directory exists with copied template utilities
- transcribe.py exports transcribe_audio() function
- format_output.py exports SRT, VTT, and text formatting functions
- All tests pass (16+ tests covering STT-01 through STT-06)
- VAD is enabled by default in transcribe_audio()
- Segment generator is properly materialized to a list
</success_criteria>

<output>
After completion, create `.planning/phases/02-stt-worker/02-01-SUMMARY.md`
</output>
