---
phase: 04-voice-cloning-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker-voice-clone/download_model.py
  - worker-voice-clone/audio_utils.py
  - worker-voice-clone/pyproject.toml
  - worker-voice-clone/validate_reference.py
  - worker-voice-clone/voice_clone.py
  - worker-voice-clone/tests/__init__.py
  - worker-voice-clone/tests/conftest.py
  - worker-voice-clone/tests/test_validate_reference.py
  - worker-voice-clone/tests/test_voice_clone.py
autonomous: true
requirements: [VC-01, VC-02, VC-03, VC-04]

must_haves:
  truths:
    - "validate_reference_audio() rejects clips under 5s with actionable error message"
    - "validate_reference_audio() rejects clips over 60s with actionable error message"
    - "validate_reference_audio() rejects audio below 16kHz sample rate"
    - "validate_reference_audio() rejects audio with estimated SNR below 15dB"
    - "validate_reference_audio() returns quality metrics dict for passing audio"
    - "clone_voice() calls Chatterbox model.generate() with audio_prompt_path and returns wav tensor + sample rate"
    - "clone_voice() passes language_id to Multilingual variant"
    - "encode_output() upsamples 24kHz to 48kHz and returns base64 string"
    - "encode_output() supports both WAV and MP3 formats"
  artifacts:
    - path: "worker-voice-clone/validate_reference.py"
      provides: "Reference audio validation with duration, sample rate, SNR checks"
      exports: ["validate_reference_audio"]
    - path: "worker-voice-clone/voice_clone.py"
      provides: "Chatterbox wrapper with model loading, cloning, and output encoding"
      exports: ["load_model", "clone_voice", "encode_output"]
    - path: "worker-voice-clone/tests/conftest.py"
      provides: "Shared fixtures: mock Chatterbox model, sample audio bytes"
    - path: "worker-voice-clone/tests/test_validate_reference.py"
      provides: "Tests for VC-03 validation logic"
    - path: "worker-voice-clone/tests/test_voice_clone.py"
      provides: "Tests for VC-01, VC-02, VC-04 cloning and encoding"
  key_links:
    - from: "worker-voice-clone/validate_reference.py"
      to: "soundfile"
      via: "sf.info() and sf.read() for metadata and waveform"
      pattern: "sf\\.info|sf\\.read"
    - from: "worker-voice-clone/voice_clone.py"
      to: "chatterbox"
      via: "model.generate() with audio_prompt_path"
      pattern: "model\\.generate\\("
    - from: "worker-voice-clone/voice_clone.py"
      to: "torchaudio.functional"
      via: "F.resample() for 24kHz to 48kHz upsampling"
      pattern: "F\\.resample\\("
---

<objective>
Copy worker-template/ to worker-voice-clone/, set up project scaffold, then implement validate_reference.py (VC-03) and voice_clone.py (VC-01, VC-02, VC-04) with full test suites.

Purpose: Builds the two core modules -- reference audio validation and Chatterbox voice cloning wrapper -- that the handler will wire together in Plan 02. Tests use mocked Chatterbox model for GPU-free execution.
Output: worker-voice-clone/ directory with validate_reference.py, voice_clone.py, shared template utilities, and passing test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voice-cloning-worker/04-RESEARCH.md

@worker-template/handler.py
@worker-template/download_model.py
@worker-template/audio_utils.py
@worker-whisper/pyproject.toml
@worker-whisper/tests/conftest.py

<interfaces>
<!-- Key utilities copied from worker-template (used verbatim, no modifications) -->

From worker-template/audio_utils.py:
```python
def resolve_audio_input(job_input: dict) -> str:
    """Resolve audio input (URL or base64) to local temp file path."""
def cleanup_audio(path: str) -> None:
    """Remove a temporary audio file after processing."""
def encode_audio_output(audio_array: np.ndarray, sample_rate: int, format: str = "wav") -> dict:
    """Encode a NumPy audio array to a base64 string with metadata."""
```

From worker-template/download_model.py:
```python
def download_model(repo_id: str, filename: str | None = None, token: str | None = None,
                   allow_patterns: list[str] | None = None, snapshot: bool = False) -> str:
    """Download model file(s) from HuggingFace with volume caching."""
```

<!-- Chatterbox API (from 04-RESEARCH.md) -->
```python
# Model loading
from chatterbox.tts_turbo import ChatterboxTurboTTS  # Turbo: 350M, English-only
from chatterbox.tts import ChatterboxTTS              # Original: 500M, English
from chatterbox.mtl_tts import ChatterboxMultilingualTTS  # Multilingual: 500M, 23 langs

model = ChatterboxMultilingualTTS.from_pretrained(device="cuda")
wav = model.generate(text, audio_prompt_path="ref.wav", language_id="en",
                     exaggeration=0.5, cfg_weight=0.5)
model.sr  # 24000 Hz native sample rate
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy template, scaffold project, and implement validate_reference.py with tests</name>
  <files>
    worker-voice-clone/download_model.py,
    worker-voice-clone/audio_utils.py,
    worker-voice-clone/pyproject.toml,
    worker-voice-clone/validate_reference.py,
    worker-voice-clone/tests/__init__.py,
    worker-voice-clone/tests/conftest.py,
    worker-voice-clone/tests/test_validate_reference.py
  </files>
  <behavior>
    - test_rejects_too_short: audio under 5s raises ValueError with "too short" and minimum requirement
    - test_rejects_too_long: audio over 60s raises ValueError with "too long" and maximum limit
    - test_rejects_low_sample_rate: audio below 16kHz raises ValueError with sample rate info
    - test_rejects_low_snr: audio with SNR below 15dB raises ValueError with quality guidance
    - test_accepts_valid_audio: 10s clean audio at 22050Hz returns dict with duration_seconds, sample_rate, snr_db, quality
    - test_quality_good_vs_acceptable: SNR >= 25dB returns quality="good", 15-25dB returns quality="acceptable"
    - test_estimate_snr_silent_noise: near-silent noise floor returns high SNR (effectively clean)
    - test_stereo_to_mono: stereo reference audio is handled correctly (uses first channel)
  </behavior>
  <action>
    1. Copy worker-template/download_model.py and worker-template/audio_utils.py verbatim to worker-voice-clone/ (same decision as Phase 2: no modifications needed).
    2. Create worker-voice-clone/pyproject.toml following worker-whisper pattern:
       - name = "worker-voice-clone", version = "0.1.0", requires-python = ">=3.10"
       - dev dep: pytest>=7.0
       - pytest config: testpaths = ["tests"], python_files = "test_*.py"
    3. Create worker-voice-clone/tests/__init__.py (empty).
    4. Create worker-voice-clone/tests/conftest.py with shared fixtures:
       - `sample_audio_bytes(duration, sample_rate, channels)` fixture factory: generates sine-wave WAV bytes using soundfile + numpy. Default: 10s, 22050Hz, 1 channel.
       - `tmp_audio_file(tmp_path, sample_audio_bytes)` fixture: writes bytes to tmp_path and returns path.
       - `mock_chatterbox_model()` fixture: MagicMock with generate() returning a torch tensor (shape [1, 24000*3] = 3 seconds at 24kHz) and sr=24000 attribute.
    5. Write failing tests in test_validate_reference.py (RED). Tests use the sample_audio_bytes factory to create valid/invalid audio files in tmp_path. For SNR tests, create audio with known signal+noise characteristics using numpy.
    6. Implement validate_reference.py following Pattern 4 from RESEARCH.md exactly:
       - Constants: MIN_DURATION_SECONDS=5.0, MAX_DURATION_SECONDS=60.0, MIN_SAMPLE_RATE=16000, MIN_SNR_DB=15.0
       - validate_reference_audio(audio_path) -> dict: checks duration, sample rate, SNR via sf.info() and sf.read()
       - _estimate_snr(audio, frame_length=2048, silence_threshold_percentile=10) -> float: energy-based SNR estimation using numpy
       - Raises ValueError with actionable messages for each failure case
       - Returns {"duration_seconds", "sample_rate", "snr_db", "quality"} on success
    7. Run tests (GREEN): `cd worker-voice-clone && python -m pytest tests/test_validate_reference.py -x -v`
  </action>
  <verify>
    <automated>cd worker-voice-clone && python -m pytest tests/test_validate_reference.py -x -v</automated>
  </verify>
  <done>validate_reference.py passes all 8+ tests. Template utilities copied. Project scaffold complete with pyproject.toml and conftest.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement voice_clone.py with model loading, cloning, and output encoding with tests</name>
  <files>
    worker-voice-clone/voice_clone.py,
    worker-voice-clone/tests/test_voice_clone.py
  </files>
  <behavior>
    - test_load_model_turbo: load_model("turbo") imports ChatterboxTurboTTS and calls from_pretrained
    - test_load_model_multilingual: load_model("multilingual") imports ChatterboxMultilingualTTS and calls from_pretrained
    - test_load_model_original: load_model("original") imports ChatterboxTTS and calls from_pretrained
    - test_clone_voice_basic: clone_voice() calls model.generate with text and audio_prompt_path, returns (wav, sample_rate)
    - test_clone_voice_multilingual_passes_language_id: when model variant is multilingual, language_id is passed to generate()
    - test_clone_voice_passes_exaggeration_and_cfg_weight: exaggeration and cfg_weight kwargs forwarded to generate()
    - test_encode_output_wav_48khz: encode_output() resamples 24kHz tensor to 48kHz, returns (base64_string, "wav")
    - test_encode_output_mp3: encode_output() produces MP3 via ffmpeg subprocess, returns (base64_string, "mp3")
    - test_encode_output_no_resample_when_same: when target_sr == native_sr, no resample call
  </behavior>
  <action>
    1. Write failing tests in test_voice_clone.py (RED):
       - Mock chatterbox imports using unittest.mock.patch to avoid requiring GPU/chatterbox installed.
       - Use the mock_chatterbox_model fixture from conftest.py for clone_voice tests.
       - For encode_output tests: create real torch tensors (small, ~0.1s at 24kHz = 2400 samples) and verify base64 output decodes to valid audio.
       - For MP3 test: mock subprocess.run to avoid requiring ffmpeg, or skip if ffmpeg not available. Better: use a small tensor and actually call ffmpeg if available, skip with `pytest.mark.skipif(not shutil.which("ffmpeg"))`.
    2. Implement voice_clone.py with three functions:
       - `load_model(variant: str) -> model`: Conditional import based on variant string ("turbo" | "original" | "multilingual"). Calls from_pretrained(device="cuda"). Returns the model object. Logs variant loaded.
       - `clone_voice(model, text, reference_audio_path, language_id="en", exaggeration=0.5, cfg_weight=0.5, temperature=0.8) -> tuple[torch.Tensor, int]`: Calls model.generate(text, audio_prompt_path=reference_audio_path, ...) passing language_id, exaggeration, cfg_weight as kwargs. Returns (wav_tensor, model.sr).
       - `encode_output(wav_tensor, native_sr, output_format="wav", target_sr=48000) -> tuple[str, str]`: Upsample via torchaudio.functional.resample() if target_sr != native_sr. Convert to numpy. WAV: encode via soundfile to BytesIO, base64 encode. MP3: write temp WAV, ffmpeg subprocess to convert, base64 encode, cleanup temps. Returns (base64_string, format_string).
    3. Run tests (GREEN): `cd worker-voice-clone && python -m pytest tests/test_voice_clone.py -x -v`
    4. Run full suite: `cd worker-voice-clone && python -m pytest tests/ -x -v`
  </action>
  <verify>
    <automated>cd worker-voice-clone && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>voice_clone.py passes all 9+ tests. load_model handles all 3 variants. clone_voice wraps Chatterbox generate. encode_output produces 48kHz WAV and MP3 base64 output. Full test suite green.</done>
</task>

</tasks>

<verification>
cd worker-voice-clone && python -m pytest tests/ -v

Expected: All tests pass (17+ tests across test_validate_reference.py and test_voice_clone.py).
Key checks:
- validate_reference_audio rejects all 4 failure cases with ValueError
- validate_reference_audio returns quality metrics for valid audio
- clone_voice correctly wraps Chatterbox model.generate
- encode_output produces 48kHz base64 output in both WAV and MP3
</verification>

<success_criteria>
- worker-voice-clone/ directory exists with download_model.py, audio_utils.py, validate_reference.py, voice_clone.py
- pyproject.toml configured with pytest
- All tests in tests/test_validate_reference.py and tests/test_voice_clone.py pass
- validate_reference.py exports validate_reference_audio with all 4 validation checks
- voice_clone.py exports load_model, clone_voice, encode_output
</success_criteria>

<output>
After completion, create `.planning/phases/04-voice-cloning-worker/04-01-SUMMARY.md`
</output>
